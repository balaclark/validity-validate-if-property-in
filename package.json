{
  "name": "validity-validate-if-property-in",
  "description": "Validate the current property if another property value is one of a list of values.",
  "version": "1.0.1",
  "dependencies": {},
  "main": "validator.js",
  "scripts": {
    "lint": "jshint .",
    "checkStyle": "jscs .",
    "pretest": "npm run-script lint && npm run-script checkStyle",
    "test": "istanbul cover _mocha test",
    "posttest": "istanbul check-coverage && rm -rf coverage",
    "prepublish": "npm test && npm prune"
  },
  "publishConfig": {
    "registry": "http://registry.npmjs.org"
  },
  "author": "Bala Clark <balaclark@gmail.com>",
  "license": "ISC",
  "devDependencies": {
    "istanbul": "^0.2.7",
    "jscs": "1.3.0",
    "jshint": "^2.5.0",
    "mocha": "^1.18.2",
    "validity": "0.0.3"
  },
  "repository": {
    "type": "git",
    "url": "git@github.com:balaclark/validity-validate-if-property-in.git"
  }
}
